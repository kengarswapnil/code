
create database hotel2;

show databases;

use hotel2;

create table login(username varchar(25), password varchar(25));
insert into login values('admin','1234');

select*from login ;


create table employee(name varchar(25),age varchar(10), gender varchar(15), job varchar(30), salary varchar(15), phone varchar(20),email varchar(40), aadhar varchar(20));

describe employee;
select * from employee;

create table driver(name varchar(20),age varchar(10),gender varchar(20),company varchar(20),brach varchar(20),available varchar(20),location varchar(40));

select*from driver;

ALTER TABLE driver RENAME COLUMN branch TO brand;
create table driver(name varchar(20),age varchar(15), gender varchar(15),company varchar (20),branch varchar(20), availible varchar(20),location varchar(40));

select *from room;

create table customer(document varchar(20), number varchar(30), name varchar(30), gender varchar(15), country varchar(20), room varchar(10), checkintime varchar(80), deposit varchar(20));
describe customer;

select * from customer;

create table department(department varchar(30), budget varchar(30)); 

insert into department values('Front office','500000');
insert into department values('Housekepping','40000');
insert into department values('Food and Beverage ','23000');
insert into department values('Kitchen or Production','540000');
insert into department values('Security','320000');



select*from department;



select * from customers; 





